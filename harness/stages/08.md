# Stage 8 — Soft Level 2 (opened 2026-09-03, CLOSED 2026-09-05)

**Closed on S8.5** (2026-09-05, operator's decision), on the condition the
operator set two days earlier and nothing else: `make fix-level FIX=2` reporting
**0 findings**. §5 is the close and the state it hands on. Like Stage 6's, this
close *is* performed by opening the successor's document — [`09.md`](09.md),
which existed as a draft since 2026-09-04 and was opened the same day.

**Scope, set by the operator on 2026-09-03: designing and implementing soft
`--fix` level 2.** This is the item Stage 6 carried forward rather than closed —
level 1 reached 0 findings on S6.11, and level 2 was left as a design question
before it is a run. The concrete content (which class or classes level 2
selects, and how) is deliberately **not decided here**: it is the next session's
work, and §1 is what that session must satisfy before any of it ships.

**How this document opened, which is unusual.** Every close from Stage 3 through
Stage 6 was performed *by* opening the successor's document. Stage 7's close
(2026-09-03) deliberately was not: the file layout was about to change, so
`STAGE8.md` was held back rather than created and immediately renamed. The
rename landed the same day — the per-stage documents now live in
`harness/stages/` as zero-padded `<NN>.md` — and this document was opened
afterwards, its scope arriving in the same session. So for this one stage,
opening the document closed nothing; Stage 7 was already closed when it
appeared.

**State at open** (the baseline every record here measures against):

- corpus **0 hard / 4,624 soft**, `make check` exits 0;
- suite **991 passed** (552 corpus + 439 harness; the 4 added after open are
  `tests/test_docs_links.py`, the repo-wide Markdown link sweep);
- soft level 1 closed at **0 findings** (S6.11);
- the live path confirmed working after S7.2's split, on the operator's
  inferno-1 re-run.

---

## 1. What level 2 has to satisfy before it runs

None of this is new — it is Stage 6's standing method, restated here because
level 2 is the first thing to be held to it from the design stage rather than
mid-flight. The next session starts from these, not from a blank page.

**The mechanism already exists.** Record S6.2 shipped the graded `--fix <level>`
run: it reopens only the units carrying that level's findings and repairs them
**in session**, showing the model the invariant and the frozen-layer evidence
for a position but never the derivation's answer (the scoped reversal of S5.5's
rule). Level 2 therefore needs a *class definition and its selection rule*, not
a new machine — and whatever it defines must be expressible in what
`extractor/fixlevel.py` can select.

**The burden of proof, from S6.1.** The soft counter's zero point is
tolerance-mediated: gold clears the bar only because 88 of the 130 registry
rules excuse the 3,250 positions where gold itself diverges from `derive_unit`,
and those tolerances were fitted by measuring that diff. So the count is a
conformance measure against derivation-plus-registry, not a quality one, and it
is not even a distance — it double-counts relocated arguments and *rises* when a
missing predicate is registered. Every candidate class must therefore be
resolved to one of three outcomes **before anything is edited**:

1. the artifact is wrong — the only outcome that licenses a fix;
2. the derivation is silent, i.e. a *tolerance* is missing (the mistake Phase 5
   kept making);
3. the two notations are equivalent.

The argument must come from `dante_corpus/skel/validate.py` and `derive.py`
**with gold unopened**. Gold-referenced scores (`make agree`) are read
afterwards as a readout, never as the acceptance criterion — Standing Invariant
§1, and the reason the project's own numbers stay meaningful.

**The candidates.** The class list and the eligibility analysis are in
[`stages/06.md`](06.md) §3, unchanged and uninvalidated by the delay. Start
there rather than re-deriving them.

**The precedent to learn from.** Level 1 took five corpus-wide runs (377 → 83 →
37 → 25 → 14 → 12 → 0) and none of S6.5's three session-side levers moved it.
What finally closed it was S6.10: `harness/`'s own transcription of
`validate.py`'s anchor rule was **narrower than the contract it transcribed**,
so the level was asking the session for rows its own gate refused. The lesson
for level 2 is to check that alignment first, not after four runs — which is
exactly what §2 item 1 below is about.

---

## 2. The two items carried in with it

Stage 7 carried three items out; the first two below bear directly on this
scope, and the third is level 2 itself (§1).

### The transcription-drift check — the check level 2 wants first (SETTLED 2026-09-05, option 3)

Investigated 2026-09-02 during Stage 7 and deliberately left unimplemented — no
test written, no code changed — because generalizing it requires deciding *what
the agent's gate ought to be*, a live authority question under Standing
Invariant §1 rather than the neutrality-bound tidy-up Stage 7 was scoped to.

The measurement is already done and **must not be repeated** (the generated
sweep costs 38 s and 25.4 M comparisons); both sweeps, their numbers and the
three options are in [`stages/07.md`](07.md) §4. In short: the committed corpus
replays through `validate_candidate` with **0 refusals** (cheap but circular),
and the non-circular sweep over 25,369,120 generated triples finds the gate
**never looser** than `validate.py` and stricter at 317,305 positions, all
inside the three clauses `runner/tools.py` already declares untranscribed (DG,
AQ, DS). No unknown drift exists. The choice was between pinning the gap with a
test, transcribing the three clauses, or scoping the property to what a `--fix`
level can select.

**Settled on the operator's decision, 2026-09-05: option 3.** The property the
harness holds itself to is *not* gate-equals-contract everywhere, but:

> whatever a `--fix` level selects, the session gate admits the row it asks for.

That is the domain where the gap can actually bite — S6.9's deadlock was a level
requiring a row its own gate refused, and nothing outside a level's selection has
ever reached a session. AQ, DG and DS stay **untranscribed**: they are
`rule_active()` registry tolerances fitted against gold's own divergence, so
transcribing them would move gold-fitted material into the *agent's* gate on
gold's authority — the thing Standing Invariant §1 exists to prevent — and the
sweep says they buy no safety we lack, since the gate is never looser than the
contract anywhere.

**What changed.** S8.1's `test_every_row_level_2_asks_for_is_admissible_to_the_session_gate`
was the property written for one level; it is now
`test_every_row_any_level_asks_for_is_admissible_to_the_session_gate`, looping
`classes_for(MAX_LEVEL)` over findings manufactured from frozen gold by both
mutations (a row deleted, a row downgraded) so that level 1 and level 2 are both
exercised — 80 + 221 and 76 + 246 checked positions in inferno 1 and 2, no
refusal anywhere, ~3 s. A class joining the table **inherits** the requirement
rather than being exempt by omission, and the assertion that every class was
exercised is what stops a level this fixture never reaches from passing
vacuously. Suite 999 → 999 (the level-2 test generalized, not added to).

**What would reopen it.** A level whose findings the gate refuses — the test
failing, in other words. That is the evidence option 2 currently lacks, and it
is the only thing that should put transcription back on the table. The cheap
replay sweep (2.5 s, 0 refusals, circular but a fair regression guard) remains
unimplemented and is still worth having; it was not part of this decision.

### The standing `skel/repairs.py` authority question (ANSWERED 2026-09-05: not used, and not a Stage 8 question)

Is `dante_corpus/skel/repairs.py` an admissible authority under discipline 3? It
opens no gold file and its rewrites are re-derivable from `derive.py` directly,
but it is the same `skel/` toolchain that built gold.
[`stages/06.md`](06.md) S6.3 priced the two routes: the deterministic route
through `repairs.py` is larger and seconds-cheap but transcribes `derive_unit`;
the live route — a session re-deriving the answer itself — is what §1 of
[`../PLAN.md`](../PLAN.md)'s autonomy premise actually measures.

**The operator's decision: `repairs.py` is not used.** What settled it is not
the authority argument but the scope: re-measured on today's corpus (in memory,
nothing written, gold unopened), what the module still offers is one rule.

| | S6.1's dry run (2026-08-30, 5,014 soft) | today (3,160 soft) |
|---|---:|---:|
| `role_label` rewrites | 371 | **0** |
| `null_subject` rewrites | 349 | **299** |
| `prep_stack` rewrites | — | 0 |
| soft after | 3,949 | 2,562 (−598) |

`role_label` went to zero because **level 1 did that work live** — the 371
mechanical rewrites are exactly the class five corpus-wide `--fix 1` runs cleared
by having the model re-derive the qualification itself. What remains is
`null_subject` alone: 299 positions, −598 soft (299 relocated arguments,
double-counted per S6.1).

And those 299 are **not level 2's**: matched against `fixlevel.select(…, 2)`,
**297 of 299 sit outside the level's selection**, in the population S8.1
deliberately argued out — a subject the derivation reaches by `conj` propagation
rather than by an argument edge, where no reading of one tree edge settles the
position. `null_subject` decides it on other evidence entirely
(`dep.subject_agreement`, a morphological agreement check).

So the question does not bear on this stage's scope at all, and considering it
now would be considering a class Stage 8 is not working on. It leaves the
carried-items list rather than staying open: it returns only if some future level
proposes to select that population, and that level would owe the class the same
contract-derived argument S8.1 gave, at which point the authority question is
about *that* class and not about the module. The measurements above are recorded
so that stage starts from numbers rather than re-running the sweep.

---

## 3. Standing rules that apply from day one

Nothing here is new to this stage; it is listed so a record written in this
document cannot claim not to have known.

- **Gold is the benchmark, never the target**, and that binds operator-side work
  too ([`../PLAN.md`](../PLAN.md) §4 item 1).
- **Session semantics are a run-scoped constant** (§6). Prompt wording may change
  between runs, never mid-run; `skill_digest` in every `canto_complete` is what
  makes that checkable after the fact.
- **The level-1 operational facts** carried out of Stage 6 — a `--fix` run cannot
  leave the corpus worse than it found it, sweep the per-canto logs before each
  corpus-wide run, `make check` exiting 0 is now the expected state — are in
  [`../PLAN.md`](../PLAN.md)'s Orientation item 6 ("Running a `--fix` level"),
  not repeated here. (They lived under §2's Stage 6 subsection until 2026-09-05,
  when PLAN.md's per-stage prose folded into a table and they moved to the
  section for facts that outlive a stage — which, having held across Stages 6 and
  8 alike, is what they are.)
- **Assistant sessions run no LLM-in-the-loop command.** Every live `--fix` run
  is the operator's.

---

## 4. Milestone Ledger

### S8.1 — Level 2 is `omitted_l4_argument`: the argument Layer 4 hangs on the predicate (2026-09-03)

The stage's design pass and its implementation. **Code and tests only; no
committed artifact was touched and running the level stays the operator's act**
(§3). The level is defined, argued from the contract with gold unopened,
implemented in `extractor/fixlevel.py`, and its gate/selection alignment was
checked *before* any run rather than after four of them — §1's reading of S6.10's
lesson.

**The class.** A `missing_arg` finding is level-2 work when the argument sits
under a Layer-4 edge from the citing predicate whose deprel is in `ARG_DEPRELS`,
and the derived role is nominal. Its authority, read gold-closed: `derive.py`'s
step 2 collects a predicate's arguments as exactly those children, so such a
child *is* an argument of that predicate on the frozen tree; the registry's 20
`missing_arg` tolerances have already been offered the position and declined it
(a finding is what survives them); and the evidence is one tree edge the model
already reads. The artifact is under-complete there — §2's **outcome 1**, the
only one that licenses an edit.

**Why the class is not all 1,639 `missing_arg` findings.** Two restrictions, both
from the contract and not from taste, measured over the committed corpus:

| population | count | why in or out |
|---|---:|---|
| argument-child of the predicate, nominal role | **1,126** | the class |
| subject reached by `conj` propagation (`derive.py` step 3) | 404 | an inference of the derivation, not an edge — no reading of one edge settles it |
| clausal role (`xcomp`/`ccomp`) at an argument-child | 74 | hard-invalid unless the argument is *also* registered as a predicate: a compound repair whose second half is `missing_tuple`'s unargued question |
| stranded on a `cop`/`aux` head (rule AM) | 1 | same reason as the propagated subject |
| under a non-argument deprel | 5 | not an argument edge at all |
| an argument-child the `holds` precondition declines | 29 | the predicate is unregistered, or the artifact already cites that position under a key `rules.py` merged away |

(1,126 + 404 + 74 + 1 + 5 + 29 = 1,639, the corpus's whole `missing_arg` class.)

By derived role the 1,126 are 788 oblique, 211 subject, 113 object, 13 indirect
object, 1 clausal subject; they sit in **770 units across all 100 cantos**, 537
units carrying one and none more than five.

**The alignment check, made first (S6.10's transferable finding).** Two halves:

1. **Can the session write the row the level asks for?** Structurally yes, and
   that is the point: every argument level 2 names sits under an `ARG_DEPRELS`
   edge, which *is* clause AF of `validate.py`'s anchor rule and of
   `tools.anchor_admits`'s transcription of it since S6.10. S6.9's deadlock — a
   level requiring a row its own gate refused — cannot arise here by
   construction, and `test_every_row_level_2_asks_for_is_admissible_to_the_session_gate`
   is what will say so if either side moves.
2. **Should the session carry a bar for this level?** **No, and this is the
   design's one refusal.** A session-side bar sees the frozen layers but not the
   registry, so the only bar it could carry is "cite every Layer-4
   argument-child of every predicate you register" — which demands **2,089**
   positions where the level selects 1,126. That is S6.10's *second* asymmetry (a
   level's bar and its selection naming different positions) at 46% of the pool
   instead of 2 units in 10, so `toolkit_flags(2)` adds nothing and level 2's ask
   lives in the notice, which names exactly the positions the checker selected.

**What crosses into the session.** The same line as level 1: the notice names the
position, the argument's Layer-1 word, and the Layer-4 relation it hangs on —
frozen-layer evidence the model already reads — plus the invariant ("an argument
the tree attaches here belongs in the predicate's frame"). The derived role is
never rendered, and a test asserts its absence in both the notice and the
assembled revision block. The notice also states the reading that declines: if
the model's own analysis makes the token something the predicate does not govern,
the frame stands.

**Mechanism changes, all small.** `FixClass.matches` now takes the canto's Layer-4
rows, because level 2 is the first class defined by the *tree* rather than by the
finding's text — the same `missing_arg` detail is this class at an argument-child
and not at a propagated subject. `dep_rows` therefore threads through
`select` / `class_of` / `governed_keys` / `fix_verdict` / `fix_diagnosis` and into
`check_canto`'s result, since a definition must be applied identically at the
readout, the plan, the acceptance test and the splice. A class that cannot see
the tree selects **nothing** rather than guessing, so a caller that forgets it
under-selects visibly instead of mis-selecting quietly; the end-to-end level-2
CLI test is the guard that the definition reaches `fix_verdict`, where losing it
would silently revert every unit as `no_improvement`.

`holds` (S6.10's precondition) declines a finding whose predicate the artifact
never registered — that is `missing_tuple`'s question — and one whose row the
artifact already carries under a key `rules.py` merged away. `keys` governs the
row the level asks for and, **for a derived subject only**, the predicate's
`(0, 0)` key: a pro-drop `subj (0, 0)` on record and an overt subject the tree
attaches are two fillings of one slot, and without that key a splice would keep
both and leave the predicate with two subjects. No other role touches `(0, 0)` —
the null position is for a dropped subject alone.

**Readouts.** `make fix-level FIX=2` reports **1,128** findings across all 100
cantos (1,126 level-2 + 2 level-1, below); suite **991 → 999**; `make check`
unchanged at **0 hard / 4,624 soft** — this record ships a level, and applying it
to the corpus is a separate act needing its own go-ahead. `FIX` still defaults to
`max`, which now resolves to 2, so an unpinned `make fix` runs both levels; pin
`FIX=1` to hold a run to level 1.

**Cost, so the go-ahead is informed.** 770 units is 2.3× level 1's first pass
(337 units, 61.5 h summed elapsed, 23.9 M tokens), and the ask is larger per
unit. A corpus-wide `--fix 2` should be budgeted at roughly **140 h summed
elapsed / ~65 h wall clock on three `-j3` streams**, extrapolated from S6.3's
per-unit rate — not measured, and the first canticle of a run will price it
better than this estimate does.

**A level-1 regression, found while measuring and left alone.** `make fix-level
FIX=1` reports **2**, not the 0 S6.11 closed at: `inferno 1` `26.2 arg (26,4)`
and `38.4 arg (38,6)`, both bare `obl` where the derivation qualifies. They came
in with the operator's live inferno-1 re-run at `31cb3c7` (the S7.2
confirmation), which is also where the corpus went 4,627 → 4,624 soft — a fresh
session re-answered that canto and re-introduced two findings of a closed level.
Verified pre-existing by running the level-1 selection under the pre-change
module. Nothing was repaired here: a level's findings are cleared by a run, not
by hand (S6.5's rule, and [`../PLAN.md`](../PLAN.md) §4 item 1). It is worth
knowing that **a closed level does not stay closed across later live runs of the
same cantos** — the level table is a standing selection, not a one-time sweep.

**What this record does not claim.** Nothing here has met a model. Level 1's arc
says the count moves on mechanism, not on prompting, and the mechanism this level
most depends on is untested in the field: the splice was built for a *relabel* and
level 2 governs a row the artifact does not have, so `governed_rows.missing` (the
answer declining to write the named row) is the diagnosis field to read first in
the run that follows — S6.9's reading order, set in advance again.

### S8.2 — the first `--fix 2` run's readout (2026-09-05)

The operator's corpus-wide `--fix 2` run (three `-j3` streams,
2026-09-02→2026-09-04, stalled and relaunched several times mid-run — resumed
cleanly each time, since a settled unit is never re-run and the TSV is the
resume state) landed between sessions. Read out here in the order §4's
reserving text fixed in advance.

**Log hygiene, checked first.** 14 of the 100 per-canto logs carried duplicate
`unit` records (28 entries total) from the stalls/relaunches. Deduped by
`(canticle, canto, line_start, line_end)` keyed by the log's path, keeping the
last — S6.7's rule — the unit count lands at exactly **770**, matching S8.1's
selection precisely; no double-counting reached the numbers below.

1. **`fix.refused.governed_rows`**, read first. 113 of the 770 units carried a
   `refused` block. Across them: `named` 176, `missing` **22**, `removed` 4,
   `untouched` 2 — a ~11% missing rate, not the high-`missing` failure mode §4
   flagged as the level's own risk. `introduced_total` off those units is 118.
   Level 2's premise (the artifact under-complete at these positions) holds at
   scale; it did not need arguing down.
2. **The refusal mix**: `accepted` 657, `salvaged` 106, `no_improvement` 3,
   `new_class:role_mismatch` 2, `new_class:extra_arg` 1,
   `new_class:extra_arg,role_mismatch` 1 (657+106+3+2+1+1 = 770). No `hard`
   verdict anywhere — the corpus-cannot-get-worse invariant held structurally,
   not just in aggregate (below). `adopted_invalid`: **1** — an ordinary schema
   failure, carrying no level-2 meaning per §4 item 2.
3. **Counts and per-class deltas.** Corpus: **0 hard / 3,160 soft** (4,624 →
   3,160, −1,464) — re-verified against a `git worktree` checkout of the
   pre-run commit rather than trusted from the working tree, and **no canto
   regressed** (all 100 checked pairwise, before ≥ after on both hard and soft
   everywhere). By kind: `missing_arg` 1,639 → 503 (−1,136, close to but not
   exactly the 1,126-finding class — some clearance is the relocated-argument
   arithmetic §4 names, not extra edits); `extra_arg` 2,097 → 1,893 (−204, the
   paired-clearance effect); `missing_tuple` 488 → 402 (−86); `role_mismatch`
   211 → 185 (−26); `membership` 144 → 134 (−10); `extra_tuple` 43 → 41 (−2);
   `dual_role` unchanged at 2. `make fix-level FIX=2` (cumulative) is now
   **12** findings (770-unit selection cleared to a residue), of which
   `FIX=1` alone is **0** — the two `inferno 1` level-1 findings this stage
   opened with are gone too, though not by hand: a fresh run of the same
   canto cleared them (the same mechanism §4's regression note describes).
4. **Cost against S8.1's estimate.** Measured from each log's real (non-report)
   `canto_complete.elapsed_seconds`, summed per canticle: inferno 16.31 h,
   purgatorio 15.14 h, paradiso 15.00 h — **summed elapsed ≈ 46.4 h**, and with
   three canticle-parallel streams, **wall ≈ 16.3 h**. Against the estimate
   (~140 h / ~65 h), actual cost is roughly a third — level 2's higher
   accepted-on-first-answer rate (657/770 ≈ 85%) likely explains most of the
   gap; level 1's harder salvage/refusal mix drove its own estimate higher.
5. **Gold agreement, last.** `make agree --per-canticle`: inferno F1 0.7640,
   purgatorio 0.7590, paradiso 0.7583, **corpus-wide 0.7605** (0.7389 → 0.7605,
   +0.0216 against S6.11). Read as a readout only, per §3/Standing Invariant §1.

Suite: **999 passed**, unchanged. `make check` exits 0.

**What this closes and what it doesn't.** The run confirms S8.1's class held up
at full scale with no session ever meeting a hard verdict, and the corpus is
materially closer to gold on a number nobody optimized for. It does **not**
close Stage 8 on its own: §2's two carried items (the transcription-drift
third option, and the `skel/repairs.py` authority question) were not forced by
this run and remain open, and the residual 12 `fix-level FIX=2` findings are a
live-run artifact rather than a repair target (S6.5's rule — cleared by a run,
never by hand).

**The close condition, set by the operator on 2026-09-05: level 2 at 0
findings.** So the residual 12 are not this level's steady-state floor — they
are what the next run has to clear, and Stage 8 stays open until it does. This
is level 1's arc again (377 → 83 → 37 → 25 → 14 → 12 → 0 over five corpus-wide
runs), with the difference that the residue is already localized: the 12 sit in
**9 cantos** — inferno 5, 9, 16, 27, 32; purgatorio 7, 10 (×4), 27; paradiso 31
— so the next pass is per-canto (`make fix-canto`), not corpus-wide, and costs
a fraction of S8.2's 16.3 h wall. Two things to expect from it, both already on
record: level 1 can re-open in any canto a fresh session re-answers (§4's
regression note — the level table is a standing selection, not a one-time
sweep), so read `make fix-level FIX=1` after the pass as well as `FIX=2`; and a
unit whose answer fails the acceptance test keeps its recorded rows, so a
residue that survives repeated passes is a mechanism question — S6.10's kind —
and not something to reach into the artifacts for.

### S8.3 — the residue read before re-running it, and the third salvage scope (2026-09-05)

S8.2 closed by saying a residue that survives repeated passes is a mechanism
question. Before spending a run on the 12, this record asks the logs which kind
of residue they are. **Code and tests only; the corpus is untouched and the
re-run is the operator's.**

**The 12 are 9 units, and one of them was already re-asked four times.** The
per-canto logs of S8.2 survive on disk, so each residual finding was traced back
to the `unit` record that produced it. `purgatorio 10` (13-21) carries four of
the twelve and appears in its log **four times**: the run stalled and was
relaunched, and a unit that never settles is re-run every time, so those are four
independent attempts at the same unit under the same prompt. All four were
refused identically, and all four wrote the same shape. Re-asking was already
tried; the answer does not change.

**What the model actually wrote**, recovered from the refused answers'
`rows.added`:

| position | derivation asks | the answer wrote |
|---|---|---|
| `15.1 obl:a (15,3)` | `obl:a` | `obl:a` ✔ |
| `15.1 obl:per (15,6)` | `obl:per` | `xcomp` ✘ |
| `16.3 obl (16,4)` | `obl` | `obl` ✔ |
| `18.8 obl:in (18,6)` | `obl:in` | `obl:in` ✔ |

Three of the four rows agree with the derivation exactly. **None of them was
written.** `salvage_rows` splices every row the level named in the unit and
`fix_verdict` measures the splice as one, so `xcomp` — a class the unit did not
carry — refused its three correct siblings with it. That is not the model failing
to reach the position; it is the mechanism discarding a repair the level itself
calls correct, which is the S6.10 shape again at a different seam.

**The whole residue, classified** (each finding against its own unit's recorded
answer, measured by splicing that one row and re-validating):

| | count | reading |
|---|---:|---|
| the row alone clears the finding | **3** | lost only to the all-rows splice |
| the row alone introduces `role_mismatch` | 3 | the label is a genuine disagreement |
| the row alone is hard-invalid | 1 | the answer is wrong there |
| no such row in the answer | 5 | 2 units whose answer was *accepted*, 2 `no_improvement`, 1 `new_class` |

**The change: a third acceptance scope.** `fixrun.salvage_by_row` runs after the
unit-scope splice is refused and takes the answer **one finding's rows at a
time**, in a deterministic order, each step measured against the state
accumulated so far and kept only when it stays hard-clean, introduces no class
the unit did not carry, and strictly lowers the level's own count. The standing
guarantee is unchanged by construction — every kept step is a measured
improvement and the whole result still goes through `fix_verdict` — so this
remains a scope at which the artifact cannot end worse than it started. Below two
named rows the scope declines: one finding's rows *are* the unit-scope splice,
and re-measuring them under a second name would only repeat that verdict. The
run reports it: a refused answer's diagnosis gains `salvage_by_row`
(`verdict`, `taken`, `offered`), and a unit saved this way is still
`verdict: salvaged`, so S6.7's summary arithmetic is untouched.

**Readouts.** Suite **999 → 1,001** (the two new end-to-end tests: a mislabelled
sibling no longer costs a correct row, and both rows mislabelled still leaves the
record verbatim). `make check` and `make fix-level FIX=2` are unmoved — nothing
in selection changed and no artifact was written. On S8.2's recorded answers the
new scope would have taken **3 of the 12**; the other 9 need a fresh session,
which is what the next per-canto pass is for.

**What this does not fix.** The 3 `role_mismatch` findings are a disagreement
about the *label*, and the level's notice deliberately never renders the derived
role (S5.5's line). If they survive the next pass, the question is which frozen-
layer evidence may be shown to settle a label — level 1's notice already shows
the `case` child that qualifies an oblique, so there is precedent to argue from,
but it is an argument to make and not an obvious extension.

### S8.4 — the second `--fix 2` pass: 12 → 1, and the residue is one label (2026-09-05)

The operator's follow-up run, launched after `467f435` and the first to use
S8.3's per-row salvage scope. Read out in the order the Handoff fixed in
advance.

**Its shape, which was not the per-canto pass S8.2 forecast.** Rather than
`make fix-canto` on the 9 cantos, the operator looped **corpus-wide `make fix`
about ten times** (9 passes over 91 cantos, 10 over the residue's 9 — counted
from the `canto_complete` records in the 2026-09-04T20:32→22:32Z window). That
is cheap by construction and the numbers show it: a canto with no finding at the
level costs no model call and closes in 0.1 s, so the whole loop came to
**≈2.6 h summed elapsed / ≈2.0 h wall**, **127 LLM requests**, **0 API
retries** — against S8.2's 46.4 h / 16.3 h. Repeating the corpus is not the
expensive gesture the per-canto advice implied; re-asking the same unit is.

**Log hygiene.** The 100 per-canto logs were *not* swept before the run, so each
of the 9 touched logs holds S8.2's records ahead of this pass's. No
reconstruction was needed: the logs segment cleanly at their `summary` records,
and every segment from 2026-09-04T20:30Z on belongs to this run (S8.2's own
relaunch segments all fall before it). The Handoff's dedupe rule was therefore
not needed either — the segments are disjoint, not duplicated.

1. **`fix.refused.salvage_by_row`, the point of the run.** It fired **exactly
   once**, at `purgatorio 10` (13-21), and it did what S8.3's simulation
   predicted to the row: `{"verdict": "accepted", "taken": 3, "offered": 4}` —
   the three rows that agreed with the derivation, taken without the `xcomp`
   that had refused them four times. Findings there went **4 → 1**, and a later
   pass cleared the last one at unit scope (`accepted`, +10 rows), so the unit
   that carried a third of the residue is now at **0**.
   The scope was `ABSENT` everywhere else, which is the design rather than a
   miss: after S8.2 every other residual unit carried a single finding, and at
   fewer than two the scope declines by construction.
2. **A `hard` verdict appeared for the first time — at the scope below the
   unit.** The same `purgatorio 10` answer recorded `"salvage": "hard"`: the
   *position*-scoped splice, taking all four named rows at once, was
   hard-invalid. No unit-scope verdict has ever been `hard` and none was here,
   but the reading matters — the all-rows splice is not merely *worse* than the
   per-row one, it can be schema-invalid where the per-row steps are clean, and
   `salvage_by_row`'s per-step hard check is what stood between that answer and
   refusal. S8.3 argued the scope from lost correct rows; this run adds that it
   also protects against a splice the intermediate scope cannot validate.
3. **The verdict mix over the run's nine units.** `accepted` 6 (inferno 5, 9,
   27; purgatorio 7, 10; paradiso 31), `salvaged` 2 (inferno 16, 32 — both at
   position scope, both introducing an ungoverned `extra_arg` at unit scope),
   and `new_class:role_mismatch` 1 (purgatorio 27), refused on every one of its
   **10** attempts. Two units needed many attempts before an answer passed:
   `inferno 5` (76-78) succeeded on its **8th**, `purgatorio 7` (107-108) on its
   **10th**, both after identical `no_improvement` refusals — so re-asking does
   sometimes converge, contra the reading S8.3 drew from `purgatorio 10`'s four
   identical answers. What distinguishes them is not attempt count but *where*
   the answers varied: all three units' answers moved from attempt to attempt at
   ungoverned rows, but at `purgatorio 27`'s governed position the answer was
   the identical `obl:in` every time — the disagreement the level selects on
   never wavered, and that is the one that never converged.
4. **Counts.** `make fix-level FIX=2`: **12 → 1**. `FIX=1`: **0** — level 1 did
   not re-open anywhere despite nine fresh passes over all 100 cantos, which is
   a stronger reading of §4's regression note than S8.2's single sweep gave.
   Corpus: **0 hard / 3,139 soft** (3,160 → 3,139, −21), `make check` exits 0,
   suite **1,001 passed** (unchanged — no code moved).
5. **No canto regressed.** Verified against a `git worktree` checkout of
   `b2d8db4` rather than the working tree (S8.2's pattern; the checkout needs
   `src/<canticle>` symlinked in, since the split texts are generated and
   gitignored). The baseline re-measured there as exactly 3,160. Eight TSVs
   changed and every one improved — inferno 5 52→50, 9 22→21, 16 37→36,
   27 28→25, 32 27→26; purgatorio 7 20→19, 10 36→31; paradiso 31 34→27 — and
   the other 92 are byte-identical, so nothing outside them could move.
6. **Gold agreement, last, as a readout.** inferno 0.7642, purgatorio 0.7591,
   paradiso 0.7586, **corpus-wide 0.7607** (0.7605 → 0.7607).

**Stage 8 does not close.** The condition is level 2 at 0 and the level reports
1: `purgatorio 27:37 missing_arg: 37.6 obl:su (38, 5)`, in unit 37-42. The
model's ten answers are not near-misses at the position — it puts the argument
at exactly `(38, 5)` every time and labels it **`obl:in` where the derivation
reads `obl:su`**, so the splice earns `role_mismatch` and the recorded rows
stand. `salvage_by_row` cannot help: one finding, one row.

So the residue is now exactly the case S8.3 said it could not fix, with nothing
else left beside it — and it is not a residue a further run reaches, because the
level's notice deliberately never renders the derived role and re-asking has now
been tried ten times on the same prompt. Whether frozen-layer evidence may be
shown to settle a *label*, and which, is the question Stage 8 has left. Level 1's
notice already renders the `case` child that qualifies an oblique, which is
precedent for showing the evidence rather than the answer; `su` vs `in` is
precisely a `case`-child reading, so the precedent is close. It is still an
argument to make against S5.5's line — and per S6.5, the finding stays a run's
work either way, never a hand repair.

> **Corrected by S8.5 (2026-09-05).** The two claims above that the residue is
> *unreachable by a run* — "it is not a residue a further run reaches" and item
> 3's reading that a governed row which never wavered across ten attempts is the
> one that never converges — are **wrong**. The operator re-ran the canto to test
> them and the eleventh attempt answered `obl:su`. Everything else in this record
> stands as measured; what failed was the inference from ten samples, not the
> samples. See S8.5.

### S8.5 — the eleventh attempt: level 2 reaches 0, and S8.4's inference was wrong (2026-09-05)

**The operator re-ran `purgatorio` precisely because S8.4 claimed a re-run could
not clear it.** That is the record's own discipline applied to a record: a claim
about what a live run can do is settled by a live run, and an assistant session
never runs one (§3). The claim did not survive.

**What happened.** Logs swept first (S6.7's rule, which S8.4's pass had skipped),
then `make fix-purgatorio`, 2026-09-05T06:20→06:24Z. One unit reopened —
`purgatorio 27` (37-42), the residue — 4 LLM requests, **192.7 s**. This time the
answer wrote `obl:su` at `(38, 5)`: the governed row, correct. The unit-scope
splice was still refused, because the same answer also added an ungoverned
`extra_arg` (`38.7 obj (37, 5)`), and the **position-scoped salvage took the
governed row alone** — `verdict: salvaged`, `governed_rows.missing` 0,
`findings 1 → 0`. So the mechanism S8.3 built and S8.4 measured is what converted
a partly-right answer into the repair, for the third time in two passes.

**Readouts.** `make fix-level FIX=2`: **1 → 0**. `FIX=1`: **0**. Corpus:
**0 hard / 3,138 soft** (3,139 → 3,138). Suite **1,001 passed**, `make check`
exits 0. `make agree`: corpus-wide **0.7607** (purgatorio 0.7591 → 0.7592;
inferno and paradiso untouched). One TSV row changed, in
`harness/recon/purgatorio/27.tsv`.

**The stage's close condition — `make fix-level FIX=2` at 0 findings — is met.**

**What S8.4 got wrong, precisely.** The observation was accurate: across ten
attempts the governed row was `obl:in` every time. The inference drawn from it —
that the position is a fixed point of the model's distribution, so re-asking is
exhausted — was not. Ten identical draws from an unknown distribution bound its
success rate loosely at best; here the eleventh draw succeeded, so the rate was
never zero, merely low. The generalization S8.4 offered alongside it ("what
distinguishes the units that converge is *where* the answers varied") is the same
error one level up: it fitted a rule to which units had happened to converge by
attempt ten, and the next attempt falsified it. The honest statement is weaker
and is what this document should carry forward:

> A `--fix` level's residue is a sample from a stochastic process. Repeated
> identical refusals lower the estimated per-attempt success rate; they never
> establish that it is zero. Only a mechanism argument — a row the splice cannot
> take, a gate that refuses what a level asks — establishes unreachability, and
> that is what S6.10 and S8.3 each actually produced.

**The label-evidence question, settled as a design ruling and then obviated.**
Asked whether the level-2 notice may render the `case` child that separates
`obl:su` from `obl:in`, the operator's answer (2026-09-05) is that **evidence
already shown at level 1 may be shown at level 2**. That is consistent with this
module's own stated line rather than an exception to it: `fixlevel.case_children()`
recomputes the `case` edge from the frozen tree specifically "so a notice cites
the tree and not the answer", and level 1's notice has rendered it since S6.2.
The ruling stands and is recorded here. It was **not implemented**, because the
finding it was for cleared without it, and the measurement taken while arguing it
says the change would be small anyway: of the corpus's 489 remaining `missing_arg`
findings, 7 have both an `obl*` derived role and a `case` child (4 more have a
`case` child at a non-oblique role, where the clause must not fire). The reason
the design question is not simply carried forward is Stage 9 — see below.

**Why this is the last level-2 record.** Not because the class is exhausted —
`make fix-level FIX=2` reports 0 against *today's* artifact, and S8.1's regression
note holds: any later live run over these cantos may re-open positions at either
level. It is the last because the stage's close condition is a count, the count is
0, and the operator closes the stage on it.

---

## 5. Stage close & what carries forward

**Closed 2026-09-05 on S8.5**, on the operator's condition of `make fix-level
FIX=2` at 0 findings and on nothing else. The stage ran five records in three
days: **S8.1** defined and implemented the level, **S8.2** and **S8.4** read out
the operator's two corpus-wide runs, **S8.3** repaired the acceptance mechanism
between them, and **S8.5** cleared the last finding and corrected S8.4.

**What the stage delivered.** Soft `--fix` level 2 as a class argued from the
contract with gold unopened (`omitted_l4_argument`), the alignment check made
*before* its runs rather than after four of them, and a third acceptance scope
(`fixrun.salvage_by_row`) that the runs then justified three times over. The
corpus moved **4,624 → 3,138 soft** at 0 hard, and the level's own count
1,128 → 0.

State at close — the baseline Stage 9 starts from:

- corpus **0 hard / 3,138 soft**, `make check` exits 0;
- `make fix-level FIX=2` **0**, `FIX=1` **0**;
- suite **1,001 passed**;
- gold agreement (readout only) **0.7607** corpus-wide — inferno 0.7642,
  purgatorio 0.7592, paradiso 0.7586 — against 0.7389 at the stage's open;
- the 100 recon TSVs committed at each step (`e5680a6`, `a466a43`, `c2cd5ec`).

**What is settled and does not carry forward.** Both items Stage 7 handed in are
answered in §2: the transcription-drift check on option 3, and `skel/repairs.py`
**not used** (on scope — 297 of its 299 remaining positions lie outside level 2's
selection, so it returns only if a future level proposes to select that
population, and then as a question about *that* class). The label-evidence
question S8.3 framed is answered as a ruling in S8.5 — evidence shown at one level
may be shown at another — and is **not** carried as work: it was never
implemented, and [`09.md`](09.md) §5 subsumes the general form of it.

Three things carry forward, none of them a task:

1. **A closed level does not stay closed.** S8.1's regression note, confirmed
   twice since: the level table is a standing selection, so any later live run
   over a canto may re-open positions at level 1 or 2. `make fix-level` is the
   cheap check, and re-running the level is the cheap repair (a canto with no
   finding costs no model call).
2. **How to read a stubborn residue.** S8.5's correction, stated generally
   because the next stage will meet the same shape: repeated identical refusals
   bound a residue's per-attempt success rate from above but never at zero. Only a
   mechanism argument — a row the splice cannot take (S8.3), a gate that refuses
   what a level asks (S6.10) — establishes that a run cannot reach something.
   Both of this stage's real advances came from that kind of argument; its one
   retracted claim came from the other kind.
3. **The re-roll cost this stage measured.** Eleven independent sessions for one
   row at `purgatorio 27`, ten at `purgatorio 7`, eight at `inferno 5` — each
   discarding the previous answer and the verdict that refused it, because a
   session that kept them would grow past the ceiling [`09.md`](09.md) §1
   measures. That is the number Stage 9's loop is meant to replace, and it is
   recorded in [`09.md`](09.md) §5 as evidence rather than left here.
